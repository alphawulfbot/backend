# Server Environment Variables

# Supabase Configuration (Required)
# Get these from your Supabase project settings (Project Settings > API)
SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
SUPABASE_ANON_KEY="YOUR_SUPABASE_PROJECT_ANON_KEY"

# Telegram Bot Configuration (Required)
# Get this from BotFather on Telegram
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"

# JWT Configuration (Required)
# Use a strong, random string (e.g., generated using openssl rand -hex 32)
JWT_SECRET="YOUR_STRONG_RANDOM_JWT_SECRET"

# Server Configuration (Optional - Render often sets PORT automatically)
# Default is 5000 if not set
PORT=5000

# Node Environment (Optional - defaults to development behavior if unset)
# Set to 'production' for deployment on Render
NODE_ENV=development

